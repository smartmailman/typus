require "test_helper"

class DeviseUserTest < ActiveSupport::TestCase

  setup do
    @devise_user = Factory(:devise_user)
  end

  test "should verify what happens when we call the name method"

end
